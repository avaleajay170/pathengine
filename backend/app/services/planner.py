"""
Roadmap Planner: orders ranked candidates by prerequisite DAG, fits weekly budget.
"""


def build_roadmap(ranked_candidates: list[dict], prerequisite_graph: dict, weekly_hours: float) -> list[dict]:
    return []


def replan(current_roadmap: list[dict], updated_gaps: list[dict], prerequisite_graph: dict, weekly_hours: float) -> list[dict]:
    return []
